## Summary

<!-- What does this pull request change, and why? -->

## Related issue

<!-- Use "Fixes #123" to close the issue automatically when merged. -->

Fixes #

## Screenshots

<!-- For visible UI changes, add before/after screenshots or a short recording. -->

| Before | After |
| ------ | ----- |
|        |       |

## Checklist

- [ ] `pnpm typecheck` passes
- [ ] `pnpm format:check` passes
- [ ] `pnpm check:boundaries` passes
- [ ] `pnpm check:release` and `pnpm test:release` pass
- [ ] `pnpm test:unit` passes, and new behaviour has tests
- [ ] `cargo fmt --check`, `cargo clippy`, and `cargo test` pass (if Rust code changed)
- [ ] All four locale files are updated (if user-facing text changed)
- [ ] An ADR is added or updated (if a boundary, dependency, schema, or product rule changed)
- [ ] No key, token, private account data, or unredacted log is included
